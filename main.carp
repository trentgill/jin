(load "Macros.carp")
(load "Interfaces.carp")

(load "lib/system.carp")
(load "lib/dout.carp")

(system-include "carp_memory.h") ;CARP_MALLOC
(system-include "string.h") ;strlen

(defn main []
  (do
    (STM32.init)
    (let [led (Dout.init)]
      (while true
        (Dout.set led 0)))))
